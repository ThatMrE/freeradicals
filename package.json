{
  "name": "freeradicals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Social feeds stay closed until you publish something. Posting buys a timed window.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "node tests/extension.e2e.mjs",
    "build": "node tools/build-manifest.mjs && node tools/make-icons.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
